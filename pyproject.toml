[project]
name = "aegis-d"
version = "0.1.0"
description = "Deterministic, auditable risk assessment kernel for autonomy research"
requires-python = ">=3.10"
dependencies = ["numpy"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
